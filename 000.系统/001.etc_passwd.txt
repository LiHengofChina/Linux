

//====================================================== /etc/passwd 文件
//====================================================== /etc/passwd 文件
	//并不存放用户的密码本身，它主要存放的是 用户的基本信息。
	//密码信息早期是存在这个文件中的，但现在出于安全考虑，密码通常存放在 /etc/shadow 文件中。


//====================================================== /etc/passwd 文件结构
//====================================================== /etc/passwd 文件结构

每一行表示一个用户，格式如下：
用户名:密码占位符:UID:GID:用户描述:主目录:登录Shell


