

1).单行注释
# 这一行是 perl 中的注释

 

2).多行注释

=pod 注释
	这是一个多行注释
	这是一个多行注释
	这是一个多行注释d
	这是一个多行注释
=cut

	/**
		注意：
			1). =pod、 =cut只能在行首，顶格写
			2). 以"=字母"开头，以"=字母"结尾。
				开始的"="后面要紧接一个字符
				结尾的"="后面可以不用。
			3). =pod 中 pod可以换成其它字符，目前没发现影响
				=cut 的cut也可以换成其它字符，
				但是 cut 换成其它字母后会导出后面的文件不再可用。
				
	*/

	